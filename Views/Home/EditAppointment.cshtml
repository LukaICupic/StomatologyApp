@*@model AppointmentEditVM

@{
    ViewData["Title"] = "EditAppointment";
}

<h1>Edit appointment</h1>

<div class="container">

    <form method="post" asp-action="EditAppointment">

        <input disabled asp-for="@Model.AppointmentId" />

        <div class="form-group row">
            <label asp-for="@Model.Title">Title</label>
            <div>
                <input asp-for="@Model.Title" placeholder="Title of the appointment" class="col-sm-2 col-form-label" />
                <span asp-validation-for="@Model.Title" class="text-danger"></span>
            </div>
        </div>

        <div class="form-group row">
            <label asp-for="@Model.ProcedureDescription">Description</label>
            <div>
                <input asp-for="@Model.ProcedureDescription" placeholder="Procedure description" class="col-sm-2 col-form-label" />
            </div>
        </div>


    </form>
</div>*@